function filterlyr() {
    var filter = new OpenLayers.Filter.Comparison({
        type: '==',
        property: 'name',
        value: 'Hungary'
    });
    var filterStrategy = new OpenLayers.Strategy.Filter();
    filterStrategy.setLayer(lyr);
    filterStrategy.setFilter(filter);
}