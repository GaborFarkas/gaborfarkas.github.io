function loadevt() {
    evt = function(){
        if (lyr.getSource().getState() === 'ready') {
            /*Do something*/
        } else {
            /*Do something else*/
        }
    };
    lyr.getSource().on('change', evt);
}